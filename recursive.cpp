#include <iostream>
#include <vector>

using namespace std;

bool address()
{



}

int main() {
    vector<int> nums;
    int n,target,tmp;
    cin >> n >> target;
    for (int i=0;i<n;i++){
        cin >> tmp;
        nums.push_back(tmp);
    }

    address(nums, );
    return 0;
}